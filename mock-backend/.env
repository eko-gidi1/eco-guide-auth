PORT=3000
REDIS_URL=redis://redis:6379 # IMPORTANT: 'redis' is the service name in docker-compose.yml
KMS_MAC_KEY_ID=mock-kms-key-id
KMS_SECRET=super_secret_kms_key_for_hmac_dont_share_this_in_prod_tornike_guramishvili

# n8n webhook URLs (Adjust if n8n is also run in the same docker-compose or externally)
# If n8n runs in the same codespace (uncommented in docker-compose.yml), it might be accessible via 'localhost:5678'
# If n8n runs outside the codespace, use its public URL.
N8N_WEBHOOK_OTP_DELIVERY=http://localhost:5678/webhook/otp-delivery
N8N_WEBHOOK_REVOKE_INCIDENT=http://localhost:5678/webhook/revoke-incident

INTERNAL_API_KEY=your_internal_api_key_for_n8n_tornike_guramiShvili